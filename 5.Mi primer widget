import 'package:flutter/material.dart';

void main() {
  runApp(new Myapp());
}

class Myapp extends StatelessWidget {
  build(context) {
    return MaterialApp(
      home: Center(
        child: Text('Hola mundo'),
      ) 
      
    );
  }
}
